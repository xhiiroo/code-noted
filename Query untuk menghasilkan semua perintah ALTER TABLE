SELECT 
  CONCAT('ALTER TABLE `', TABLE_NAME, '` MODIFY COLUMN `', COLUMN_NAME, '` VARCHAR(20);') AS sql_statement
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_SCHEMA = 'nama_database'
  AND COLUMN_NAME = 'nama_kolom';
